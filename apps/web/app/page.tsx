import React from "react";
import CanvasComponent from "./components/Canvas";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <CanvasComponent />
    </div>
  );
};

export default page;
